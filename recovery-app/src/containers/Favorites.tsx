import styled from "styled-components";

const StyledFavorites = styled.div`
  width: 90vw;
  height: 100vh;
  position: fixed;
  top: 0;
  right: 0;

  background-color: #a1b888;
`;

export const Favorites = () => {
  return <StyledFavorites>.</StyledFavorites>;
};
